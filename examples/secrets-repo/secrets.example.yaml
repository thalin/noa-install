# Decrypted template for the demo secrets.yaml. This file is plaintext on
# purpose (note the ".example" suffix) - it's what you encrypt to produce the
# real, sops-encrypted secrets.yaml the demo uses. See examples/README.md.
example_secret: "replace-me"
