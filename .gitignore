result
result-*
.direnv/
.pytest_cache/
__pycache__/

# Demo private key (throwaway, regenerate with:
# ssh-keygen -t ed25519 -f examples/demo-ssh-key -N "" -C noa-install-demo)
examples/demo-ssh-key
!examples/demo-ssh-key.pub

# Demo target VM's disk image and the operator's SSH known-hosts scratch file
*.qcow2
examples/known_hosts

# Generated by running the demo (examples/README.md): demo age identity,
# the real encrypted secrets.yaml, and per-host sops-encrypted ssh keys.
# Only .sops.yaml and secrets.example.yaml are meant to be tracked.
examples/secrets-repo/admin-key.txt
examples/secrets-repo/secrets.yaml
examples/secrets-repo/systems/
